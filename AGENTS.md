# Manna Markdown Machine router

This file is a thin, non-authoritative host router. It creates no project
authority and carries no task, lifecycle, STOP, review, repository, approval, or
handoff state.

Begin recovery at `.markdown-machine/HANDOFF.md`, then read
`.markdown-machine/RUNTIME.md` and the exact contracts exported under
`.markdown-machine/contracts/`. The project-local `.markdown-machine/` closure
is canonical; this router is not referenced by any governed record.
