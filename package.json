{
  "name": "manna",
  "version": "0.0.0",
  "private": true,
  "description": "Offline-first comic-themed Bible study workstation in a single self-contained HTML file",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "lint": "node scripts/lint.js",
    "test": "node --test test/",
    "verify": "npm run lint && npm test && npm run build",
    "verify-vendor": "node scripts/verify-vendor.js"
  },
  "devDependencies": {}
}
